version: '2'
services:
  imanagestuff-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/imanagestuff/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=imanagestuff
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
